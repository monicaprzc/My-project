using JetBrains.Annotations;
using Unity.VisualScripting.Antlr3.Runtime.Tree;
using UnityEngine;

public class Animal : MonoBehaviour
{
    public virtual void Eat()
    {

    }
     /*private string name;
    public Animal(string _name, Color _color, float _weight)
    {
        name = _name
    }
    
     /*public string _name;
   private float _weight;
   protected Color _color;

   public void ejemploPublic(int _damage, string _attackName)
   {
    _damage++;
   }
   private void ejemploPrivate()
   {
    
   }

   protected void ejemploProtected()
   {
    
   }

   public int hungerLevel = 5;
    private int _currentHungerLevel; 

    private void Start ()
    {
        Debug.Log(Eat());
        Debug.Log(Eat());
        Debug.Log(Eat());
        Debug.Log(Eat());
        Debug.Log(Eat());

    }

    private bool Eat ()
    {
        _currentHungerLevel++;
        if (_currentHungerLevel >= hungerLevel)
        {
            Debug.Log("Ya me llené");
            return true;
        }
        else
        {
            Debug.Log("Aún tengo hambre");
            return false;
        }
    }
*/
}
